[package]
name = "cedarpy_conversor"
version = "0.0.2"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "cedarpy_conversor"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }
cedar-policy = "2.4.2"
serde = { version = "1.0", features = ["derive", "rc"] }
serde_json = "1.0"
